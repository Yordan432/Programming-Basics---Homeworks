using System;

namespace TriangleArea
{
    class TriangleArea
    {
        static void Main(string[] args)
        {
            var side = double.Parse(Console.ReadLine());
            var geight = double.Parse(Console.ReadLine());
            var area = side * geight / 2.0;
            Console.WriteLine(area);
        }
    }
}
