using System;

namespace Money
{
    class Money
    {
        static void Main(string[] args)
        {
            int workdays = int.Parse(Console.ReadLine());
            double money = double.Parse(Console.ReadLine());
           double fullmoney = double.Parse(Console.ReadLine());

            var result = workdays * money;
            var yearsmoney = result * 12 + result * 2.5;
            var tax = 0.25 * yearsmoney;
            Console.WriteLine(tax);
        }
    }
}
