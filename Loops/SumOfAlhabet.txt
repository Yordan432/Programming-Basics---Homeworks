using System;

namespace SumOnAlhabet
{
    class SumOfAlhabet
    {
        static void Main(string[] args)
        {
            string alhabet = Console.ReadLine();

            if(alhabet == "a")
            {
                if(alhabet == "hello")
                {
                    int numTwo = int.Parse(Console.ReadLine());
                    int numFour = int.Parse(Console.ReadLine());
                    Console.WriteLine(numTwo + numFour);
                }
            }

        }
    }
}
